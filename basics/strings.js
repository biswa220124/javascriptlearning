const repo = 69;
const name = "Biswa";
// console.log(`My name is ${name} and i have ${repo} repositries on Github`);
const city = new String();
const bigWord = new String(`The Big Brown Fox Has Twenty Kids    `);

console.table([bigWord.charAt(5), bigWord.replaceAll('B','D'), bigWord.trimEnd(), bigWord.slice(2,4)]);
